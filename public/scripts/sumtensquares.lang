function pow(base, exponent) {
  i32 i;
  i32 total = 1;
  for (i = 0; i < exponent; i++) {
    total = total * base;
  }
  return total;
}

function sumtensquares() {
  i32 i;
  i32 total;
  total = 0;
  for (i=0; i<10; i++) {
    total = total + y(i, 2)
  }
}